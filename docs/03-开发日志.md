# 开发日志

## 2026-06-12 项目启动（v0.1）

**需求**（Walter）：自己的中英文输入法；统计按键次数、中文/英文字数；游戏内不受影响；体验对标微软拼音。

**决策**：
- 技术路线选定"Rime 引擎定制"（用户确认），放弃从零写 TSF——理由见 01-方案设计.md
- 统计展示选定"托盘图标 + 弹出面板"
- 中文方案：雾凇拼音（rime-ice）为主，朙月拼音简化字兜底

**完成**：
- rime-config：default/weasel custom 配置、游戏 app_options（14 个常见游戏进程）、commit_logger.lua 上屏日志
- stats-app：ime_stats.py（pynput 钩子 + 日志读取 + SQLite + pystray/tkinter 面板）
- scripts：rime-ice 一键安装、配置一键部署（带备份）
- docs：方案设计、安装指南

**自查修复**（提交前代码审查发现）：
- 钩子回调内原本直接调 Win32 查询（最多阻塞 50ms）——低级钩子回调阻塞会拖慢全系统按键，违背"游戏无影响"目标。已改为回调只入队，统计逻辑移到工作线程，并对进程名/IME 模式查询做 1s/0.2s 缓存
- `SendMessageTimeoutW` 的 lpdwResult 原用 32 位 DWORD，64 位系统下会越界写，改为 `c_size_t`
- 托盘图标 PIL 默认字体不支持中文"字"，改为加载 msyh.ttc 并留降级

**待办 / 已知问题**：
- [ ] IME 中英模式检测在纯 TSF 场景可能失败（英文数轻微高估），观察实际效果
- [ ] 后续可选：统计面板加图表、C# 重写统计程序降内存

## 2026-06-12（下午）远程部署完成

沙盒恢复可用，已代用户完成部署：

- **语法校验**：4 个 yaml 全部通过 PyYAML 解析；ime_stats.py 通过 py_compile；commit_logger.lua 结构检查通过
- **git**：挂载盘不支持 git 的原子改名/删除（.git/config 被写为 NUL、锁文件无法删除）→ 改用"仓库在沙盒 + bundle 备份在项目根"方案。`ime-repo.bundle` 即完整仓库，恢复方法：`git clone ime-repo.bundle IME-repo`
- **配置部署**：已获 %APPDATA%\Rime 访问授权，原 default/weasel.custom.yaml 备份至 `backup_20260612/`，本项目 6 个配置文件已写入
- **雾凇拼音**：沙盒内 git clone 成功（curl 被限制），66MB 方案+词库已全量复制进 Rime 目录（排除 build/others/文档），关键文件齐全，与我们的 lua/custom 无冲突
- scripts/ 两个 ps1 保留，供日后重装系统时使用

## 2026-06-12（晚）v0.2 面板实时刷新

用户实测反馈：输入法好用；统计面板打开后数字不动。

- 面板改为每秒刷新：显示值 = SQLite 已落盘 + KeyCounter 未落盘增量（新增 `snapshot()`，读不清零），中文字数靠每秒 `reader.poll()` 增量拉取上屏日志
- 数字加千分位；最近 7 天同步刷新；重复点托盘图标改为前置已开面板而非重建
- 修复隐患：面板线程与 30s 落盘线程并发调 `poll()` 可能重复计数 → CommitLogReader 加锁
- 用户更新方式：托盘退出 → 重新 `pythonw ime_stats.py`（数据库无变化，无需迁移）

## 2026-06-12（晚）v0.3 主题与面板 UI

用户反馈：候选字体太紧凑，想要圆润轻盈、配色简单。

- **walter_light 输入法主题**（weasel.custom.yaml）：幼圆→雅黑Light 字体回退链、14pt、白底灰字单蓝高亮、序号去点弱化、圆角10/12、margin/spacing/candidate_spacing 全面加大、淡阴影。已同步部署到 Rime 目录，待用户「重新部署」生效
- **统计面板卡片化**：浅灰底+白卡片，今日值 20pt 大数字（中文字数蓝色强调），卡片内附累计值，最近7天对齐排版。实时刷新逻辑不变
- 备注：corner_radius/shadow 需较新版小狼毫，旧版会忽略不报错；若"幼圆"缺失自动退雅黑 Light

## 2026-06-12（晚）v0.4 字号调小 + 开机自启脚本

- 用户反馈主题字号偏大：font_point 14 → 12，已同步部署到 Rime 目录（待重新部署生效）
- 开机自启：「启动」文件夹是受保护路径无法直接写入 → 新增 `scripts/enable_autostart.bat`，双击一次即把自启 vbs（含绝对路径）写入 shell:startup；取消自启删除该 vbs 即可

## 2026-06-12（夜）v0.5 自启修复 + 面板图表 + 单实例

- 用户双击自启脚本后出现 Python 交互解释器窗口（>>> 提示符）= 启动命令丢失脚本参数。重写 enable_autostart.bat：`where pythonw` 取绝对路径写入 vbs、校验脚本存在、写完即测试启动
- ime_stats.py 加互斥锁单实例保护（防双开双计数，重复启动直接退出）
- 统计面板新增最近 14 天柱状图×2（按键/中文字数），tk.Canvas 绘制，今日柱实时并入内存增量，数据变化才重绘
- **挂载坑+1**：ime_stats.py 在沙盒侧持续读到截断的旧版本（13671B 卡死，Windows 侧完好），等待/touch 均无效。解法：完整内容写成新文件（新文件同步正常）→ 沙盒校验 → `cp` 覆盖回原名（沙盒→Windows 方向同步正常）。今后大改此文件建议直接整文件重写而非 Edit

## 2026-06-12（夜）v0.6 四项增强（用户选定）

用户从优化清单选了：时段分布、常用词排行、词库自动更新、每周报告、C#重写。前四项本次完成，**C#重写暂缓**（工作量最大，等功能稳定后整体迁移，接口不变：同一 stats.db + commit_log.txt）。

- **时段分布**：新增 hourly 表（date,hour,keys,cn_chars)；按键在工作线程按小时归账，中文按日志行自带时间戳归账（跨天也准确，并顺手修了积压日志全记到今天的小 bug）；面板新增"今日时段分布"24柱图（暖橙色）
- **常用词排行**：新增 word_freq 表；2~6 个纯汉字的整体上屏算一个词；面板底部显示 Top 10
- **每周报告**：启动时后台检查上一自然周（周一~周日）报告是否已生成，缺则写 `docs/reports/输入周报-YYYY-Wxx.md`（总量、环比、逐日表格、常用词），meta 记录防重复
- **词库自动更新**：install_rime_ice.ps1 升级——更新后自动定位 WeaselDeployer.exe 触发重新部署，并保护 rime.lua 不被覆盖；新增 enable_weekly_dict_update.bat 注册每周日 12:00 计划任务
- 数据库改动向后兼容（CREATE TABLE IF NOT EXISTS），旧 stats.db 直接沿用；时段/词频数据从启用时刻开始积累
- **挂载坑+2**：对已被沙盒读过的文件做 Edit/Write，沙盒会读到旧版甚至把旧版提交进 git（本次 install_rime_ice.ps1、开发日志均中招，靠 `git show HEAD:` 核验发现）。固化工作法：改完必须 `git show` 核验提交内容，失败则走"新文件名→cp 覆盖"

**剩余两步需用户在 Windows 上操作**（沙盒是 Linux，无法执行）：
1. 右键任务栏小狼毫图标 →「重新部署」（首次编译词库约 1-2 分钟）
2. 启动统计程序：`cd stats-app; pip install -r requirements.txt; pythonw ime_stats.py`

## 2026-06-12（夜）v0.7 jieba 分词 + 关闭切换提示

用户反馈两问题：①"添加一个新功能吧"整句被当成一个词（旧算法按整次上屏计词）②中英切换时光标旁弹出图标。

- **分词**：常用词改用 jieba 分词，取 ≥2 字纯汉字词，单字助词自然排除；未装 jieba 退化为旧算法并在面板提示。启动时检测 word_seg_ver≠2 且 jieba 可用 → 清空 word_freq 从完整日志重建（旧的"整句词"数据洗掉）。requirements.txt 加 jieba
- **切换提示**：weasel.custom.yaml 加 style/show_notifications: false + show_notifications_time: 0，已部署到 Rime 目录，待重新部署生效
- 沙盒无法安装 jieba（pip 网络受限），py_compile 通过；分词效果待用户机实测
- 开发日志改用沙盒侧 cat >> 追加，避开 Windows→沙盒同步失效问题
